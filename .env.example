# docker-compose.deploy.yml 用。このファイルを .env にコピーして値を設定する (.env はコミットしない)
# Cloudflare Zero Trust > Networks > Tunnels で作成したトンネルのトークン
CLOUDFLARE_TUNNEL_TOKEN=

# 使用するイメージのタグ (省略時は latest)。例: sha-1a2b3c4
# IMAGE_TAG=latest
