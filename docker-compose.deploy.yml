# 本番 (自宅サーバー) 用: ghcr.io のイメージ + Cloudflare Tunnel で公開する
#   docker compose -f docker-compose.yml -f docker-compose.deploy.yml pull
#   docker compose -f docker-compose.yml -f docker-compose.deploy.yml up -d
# 事前に同じディレクトリの .env に CLOUDFLARE_TUNNEL_TOKEN を設定しておく (.env.example 参照)
# Cloudflare 側の Public Hostname のサービスは http://react-frontend:80 を指定する
services:
  # 公開は Cloudflare Tunnel (+ Cloudflare Access) 経由のみにする。
  # ホストのポートを開けると Access を通らずに直接アクセスでき、レート制限キー (CF-Connecting-IP) も偽装できるため、
  # ベースの docker-compose.yml の "4174:80" を打ち消す (Docker Compose v2.24 以降の !reset タグ)。
  react-frontend:
    ports: !reset []

  cloudflared:
    image: cloudflare/cloudflared:latest
    restart: unless-stopped
    command: tunnel --no-autoupdate run
    environment:
      - TUNNEL_TOKEN=${CLOUDFLARE_TUNNEL_TOKEN:?CLOUDFLARE_TUNNEL_TOKEN を .env に設定してください}
    depends_on:
      - react-frontend
    mem_limit: 128m
